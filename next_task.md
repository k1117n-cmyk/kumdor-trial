# クムドールの試練 今後の作業計画

この文書は、`v0.4.0` の戦闘イベント強化後に進める作業候補を、全体工程とゴールが見えるように整理したものです。

## 現在地

現在のクムドールの試練は、C言語製のターミナルタイピングRPGとして、最初から最後まで遊べる状態です。

完了済みの主な内容:

- 10ステージ制のタイピングRPG
- 各ステージは基本10問で突破する敵HP
- ステージ別の敵、場所、物語文、練習内容
- 英字、数字、記号、スペース、大文字・小文字を含む課題入力
- ターン制バトル
- HP、経験値、レベルアップ
- 毒と暗闇の状態異常
- 暗闇時の入力非表示
- スコア表示
- ステージ突破時と終了時のステージ別成績表示
- 敵HPが半分以下になると発生する敵の本気状態
- 敵ごとに異なる反撃特性
- 敵ごとの短い口上
- 3連続正解ごとの追加ダメージ
- 本気状態中の敵の予告行動
- ステージ突破時の自動セーブ
- `:save`、`:quit`、`:savequit` による中断操作
- `:help`、`:commands`、`:?` によるヘルプ表示
- `:bgm`、`:mute` によるゲーム中のBGM切り替え
- README、Makefile、MITライセンス
- macOSでのステージ別BGMループ再生
- `KUMDOR_NO_BGM=1` によるBGMなし起動
- `KUMDOR_BGM_VOLUME` によるBGM音量調整
- `KUMDOR_SAVE_V2` によるステージ別成績の保存
- 音声、セーブ、戦闘処理のファイル分割

現在の到達点は「小さな公開ゲームとして成立する状態」です。今後は、遊びやすさ、保守性、公開品質を上げていく段階です。

## 最終ゴール

最終的には、GitHub上で以下を満たすターミナルRPGにすることを目指します。

- 初めて触る人がREADMEだけでビルドして遊べる
- 最初から最後まで自然な難易度で遊べる
- タイピング練習として成績を振り返れる
- セーブデータや設定変更に強い
- macOSではBGM込みで遊べる
- BGMなし環境やmacOS以外でもゲーム本体は動く
- コードが今後の追加に耐えられる程度に整理されている

## フェーズ1: v0.4.0 の仕上げ

目的: BGM対応と戦闘イベント強化後の状態を安定させ、公開できる単位にまとめる。

作業候補:

- v0.4.0 の変更を小さなコミットにまとめる
- 必要ならリリースタグを作る

完了済み:

- `make` が警告なしで通ることを確認済み
- BGMあり、BGMなし、カラーなし起動を確認済み
- `BGM/kumdor_01.wav` から `BGM/kumdor_10.wav` まで配置済み
- `articles/` と `game.log` は `.gitignore` に追加済み

完了の目安:

- READMEとHANDOFFの内容が実装と一致している
- 生成物やセーブデータがGit管理に入らない

## フェーズ2: BGMまわりの改善

目的: BGMを使いやすくし、遊ぶ環境に合わせやすくする。

完了済み:

- `KUMDOR_BGM_VOLUME` で `0.0` から `1.0` の音量指定に対応
- 未指定、空文字、数値でない値、範囲外の値は既定値 `0.45` に戻す
- BGM開始ログに、実際に使う音量を表示する
- `:bgm` / `:mute` でゲーム中にBGMを切り替えられる
- `KUMDOR_NO_BGM=1` の場合はゲーム中のBGM切り替えも無効にする
- BGM再生開始に失敗した場合は、無音で続行することが分かる警告を表示する
- READMEに音量指定の実行例を追加

完了の目安:

- 音量を環境変数で変更できる
- BGMなしでもログがうるさくならない
- 音声がなくてもゲーム進行に影響しない

## フェーズ3: セーブデータの強化

目的: 今後の仕様追加で古いセーブデータが壊れにくいようにする。

作業候補:

- 今後セーブ項目を増やす場合は、`KUMDOR_SAVE_V3` として互換ロードを維持する
- セーブデータ検証のテスト方法を決める

完了済み:

- `KUMDOR_SAVE_V2` の形式識別子を保存する
- 古い `KUMDOR_SAVE_V1` のセーブデータも読み込める
- `KUMDOR_SAVE_V2` ではステージ別成績も保存する
- 形式識別子が違うセーブデータは新規開始にする
- 読み込み失敗時は新規開始にする
- 不正なHP、レベル、経験値、状態異常値、ステージ別成績を含むセーブデータは新規開始にする
- クリア済みセーブデータは新規開始にする
- READMEにセーブ形式識別子と不正データ時の扱いを追記

完了の目安:

- セーブ形式を変更しても読み込み処理の判断が明確
- 不正なセーブデータでゲームが不自然に進まない
- READMEと実装の説明が一致している

## フェーズ4: タイピング練習としての改善

目的: クリア結果だけでなく、入力の正確さを自分で振り返れるゲームにする。

作業候補:

- 入力失敗とタイプミスの違いが分かる表示にする
- ステージごとの課題数や難易度を調整する
- 後半ステージの長い課題が理不尽になっていないか確認する
- 敵が本気を出した後に出る課題の長さを実プレイで調整する
- 敵ごとの反撃特性が厳しすぎないか実プレイで調整する

完了済み:

- 敵HPが半分以下になると「敵の殺気」メッセージを表示する
- 敵が本気を出した後はステージごとの後半用課題セットから出題する
- 後半用課題を通常課題の後半流用から専用配列へ変更する
- 各ステージの敵HPを10にそろえ、敵が本気を出した後も5問前後続くようにする
- 敵ごとの反撃特性を追加する
- 戦闘開始時に敵特性と効果説明を表示する
- 戦闘開始時に敵ごとの短い口上を表示する
- 3連続正解ごとの追加ダメージを追加する
- 本気状態中に敵の予告行動を表示する
- 初期状態で暗闇にならないようにする
- 暗闇を正解で解除される一時効果にする
- ステージ3以降はミス時の共通ペナルティとして暗闇を付与する
- 暗闇特性の敵は本気状態中にもランダムで暗闇を付与する
- 初期状態で毒にならないようにする
- 毒を敵の反撃と本気状態中のランダム発生に連動させる
- 毒と暗闇をセーブデータに持ち越さない一時効果にする
- ステージ別正解数、ミス数、入力失敗数を記録する
- ステージ別成績を `KUMDOR_SAVE_V2` で保存・ロードする
- ステージ突破時に、そのステージ単体の正解数、ミス数、入力失敗数、命中率を表示する
- 終了時スコアに、入力があったステージだけステージ別成績を表示する
- 命中率が一番低いステージを、指示ではなく成績情報として表示する

完了の目安:

- クリア後にステージ別の成績を確認できる
- 序盤は練習しやすく、後半は手応えがある
- ミス時のダメージが厳しすぎず、緩すぎない

## フェーズ5: 表示とテンポの改善

目的: ターミナル上で読みやすく、続けやすい体験にする。

作業候補:

- 戦闘ログの表示量を整理する
- ステージ開始、戦闘開始、ステージ突破、スコア表示の見た目をそろえる
- HP、状態異常、敵HPの表示をより読みやすくする
- BGM開始やセーブ完了などの補助ログを控えめにする
- クリア時と敗北時の演出を少し強化する

完了の目安:

- 画面を見たときに、次に入力すべき課題がすぐ分かる
- 重要な情報と補助情報の区別がつく
- 長時間遊んでもログが読みづらくならない

## フェーズ6: コード整理

目的: 今後の追加作業をしやすくする。

作業候補:

- 共有する関数宣言を `include/game.h` から適切に整理する
- ファイル分割後も `make` で簡単にビルドできる状態を保つ
- 表示用カラー定義の重複を整理する

完了の目安:

- `src/game.c` がゲーム全体の流れを追いやすい大きさになる
- 音声、セーブ、戦闘の責務が分かれている
- 分割によってビルド手順が複雑になっていない

完了済み:

- `src/audio.c` と `include/audio.h` にBGM制御を切り出す
- `src/game.c` はBGM開始、停止、終了時クリーンアップを呼ぶだけにする
- `src/save.c` と `include/save.h` にセーブ、ロード、セーブデータ検証を切り出す
- `src/battle.c` と `include/battle.h` に戦闘表示、入力判定、ターン処理を切り出す

## フェーズ7: 公開品質の仕上げ

目的: GitHubで見た人が安心して試せる状態にする。

作業候補:

- READMEに既知の制限を書く
- ヘルプ表示の文言を実プレイに合わせて調整する
- macOS以外ではBGMなしで動くことを明記する
- リリースタグを作る
- 変更履歴を作る
- 必要ならスクリーンショットや短いプレイログを追加する
- 公開記事側の更新が必要な場合は、リポジトリ外の原稿として管理する

完了の目安:

- READMEだけでビルド、実行、BGMなし起動、セーブ操作が分かる
- 公開時の説明と実装内容がずれていない
- `v0.4.0` 以降の更新内容を追える

## 優先順位

次に取りかかるなら、以下の順番が扱いやすいです。

1. 現在の変更をコミット単位に整理する
2. 敵ごとの反撃特性の実プレイ調整
3. 戦闘ログの整理
4. ステージごとの課題バランス調整
5. 表示用カラー定義の整理

## すぐ着手しやすい小タスク

- スコア表示に到達ステージをより分かりやすく出す
- セーブ成功メッセージとBGMメッセージの表示位置を整理する
- READMEに既知の制限を書く

## 判断が必要なこと

- BGMファイルをGitHubに含めるか、別配布にするか
- macOS以外のBGM対応を将来行うか
- ゲーム内設定コマンドを増やすか、環境変数中心にするか
- 次のリリースタグを `v0.4.1` にするか `v0.5.0` にするか
