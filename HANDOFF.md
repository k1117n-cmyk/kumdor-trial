# クムドールの試練 引き継ぎ書

この文書は、`kumdor_01.c` をターミナルRPGとして育てていくための作業引き継ぎメモです。

## 現在のプロジェクト状態

このリポジトリは Git 管理済みです。

直近までに完了していること:

- 初回コミット作成済み
- `kumdor_01.c` を `Player` / `Enemy` の `struct` ベースに整理済み
- ターン制バトルの土台を追加済み
- プレイヤーHP、敵HP、敵反撃、勝利、敗北を実装済み
- 小文字入力にも対応済み
- `kumdor_01_explanation.md` にコード解説を作成済み
- `Makefile` と `README.md` を追加済み

現在、コミット済みの主要ファイル:

- `kumdor_01.c`
- `kumdor_01_explanation.md`
- `Makefile`
- `README.md`
- `HANDOFF.md`

次に作業を始める前に、まず以下を確認してください。

```sh
git status --short
make
make run
```

## 現在のファイル構成

```text
.
├── .gitignore
├── HANDOFF.md
├── Makefile
├── README.md
├── kumdor_01.c
└── kumdor_01_explanation.md
```

`kumdor_01` はビルド生成物なので Git 管理しません。

## ビルドと実行

ビルド:

```sh
make
```

実行:

```sh
make run
```

生成物削除:

```sh
make clean
```

警告つきでコンパイルする設定は `Makefile` に入っています。

```make
CFLAGS := -Wall -Wextra -pedantic
```

## 現在のゲーム仕様

現在のゲームは、1戦だけのタイピングRPGです。

基本ルール:

- ランダムなアルファベットが1文字表示される
- プレイヤーはその文字を入力する
- 大文字でも小文字でも正解になる
- 正解すると敵にダメージを与える
- 敵HPが0になると勝利
- 敵が生きていれば反撃する
- プレイヤーHPが0になると敗北

状態異常:

- 毒: 正解してもダメージを与えず、毒が解除される
- 暗闇: 正解文字が小文字で表示される

現在のバランス:

| 項目 | 値 |
| --- | --- |
| プレイヤーHP | 10 |
| 敵名 | クムドールの影 |
| 敵HP | 3 |
| 敵攻撃力 | 2 |
| 通常正解ダメージ | 1 |

## 当面の進め方

当面はソースファイル分割を急がず、`kumdor_01.c` のままゲーム性を固めます。

おすすめ順:

1. `Makefile` と `README.md` と `HANDOFF.md` をコミットする
2. 1文字入力から単語入力へ変更する
3. 敵データを複数用意する
4. ステージ制を追加する
5. 経験値とレベルアップを追加する
6. セーブとロードを追加する
7. コードが大きくなってからファイル分割する

## 次の作業候補 1: 単語入力化

現在は `char target` と `char input` の1文字判定です。

単語入力にする場合の変更方針:

- `char words[26]` を文字列配列に変更する
- `choose_target` の戻り値を `const char *` にする
- `read_input` で文字列を読む
- `toupper` による1文字変換から、大文字小文字を無視した文字列比較に変更する

例:

```c
const char *words[] = {
    "SWORD",
    "MAGIC",
    "SHIELD"
};
```

この段階では、まだ外部ファイル化しなくてよいです。まずはCファイル内の配列で十分です。

## 次の作業候補 2: 敵データの複数化

現在は敵が1体だけです。

```c
Enemy enemy = {"クムドールの影", 3, 3, 2};
```

複数敵にする場合は、まず配列で管理します。

```c
Enemy enemies[] = {
    {"クムドールの影", 3, 3, 2},
    {"霧の番人", 5, 5, 3},
    {"古文書の魔術師", 8, 8, 4}
};
```

この段階では、敵を順番に出すだけで十分です。

## 次の作業候補 3: ステージ制

複数敵が入ったあとで、ステージ制を追加します。

最初の形:

- ステージ1: 弱い敵
- ステージ2: 少し強い敵
- ステージ3: ボス

複雑なマップや移動はまだ不要です。公開ゲームを目指すとしても、まずは戦闘のテンポを固めるのが優先です。

## ファイル分割のタイミング

現時点では、まだファイル分割は不要です。

分割を検討する目安:

- `kumdor_01.c` が300行を超えた
- 戦闘、入力、敵データ、セーブ処理の役割が明確に分かれた
- 同じような処理が何度も出てきた
- テストしやすい構造にしたくなった

分割する場合の最初の候補:

```text
src/main.c
src/game.c
include/game.h
```

最初から `battle.c`, `player.c`, `enemy.c` まで細かく分けすぎないほうが進めやすいです。

## コミット方針

コミットは小さく分けます。

例:

```text
Add project README and Makefile
Support word typing
Add multiple enemies
Add stage progression
Add player leveling
```

1つのコミットには、できるだけ1つの目的だけを入れます。

作業前:

```sh
git status --short
```

作業後:

```sh
make
git diff --stat
git status --short
```

## 注意点

- `kumdor_01` は実行ファイルなのでコミットしない
- `make clean` は生成物を消すために使う
- ゲーム仕様を変えたら `README.md` と `kumdor_01_explanation.md` も必要に応じて更新する
- 乱数を使っているため、手動確認では毎回出る文字や状態異常が変わる
- 入力処理を変更するとゲーム全体に影響しやすいので、必ず `make run` で確認する

## 公開に向けた長期方針

まずはターミナル版として最後まで遊べる形にします。

公開前に必要になりそうなもの:

- 安定したビルド手順
- 遊び方が分かる README
- ライセンス
- スクリーンショットまたはプレイ例
- 既知の制限
- リリース用タグ

最初の公開目標は、GitHub上で「ビルドして遊べるC製ターミナルRPG」として成立する状態です。

## GitHub公開タイミング

GitHub公開は、次の3段階で考えます。

| タイミング | 位置づけ |
| --- | --- |
| 今すぐ | 開発ログ、学習記録としての公開 |
| 単語入力化後 | タイピングゲームとして最低限見せられる公開 |
| 単語入力、複数敵、ステージ制の実装後 | 小さな公開ゲームとしての公開 |

このプロジェクトでは、3つ目の「単語入力、複数敵、ステージ制の実装後」を最初の公開目標にします。

公開前チェック:

- `make` が警告なしで通る
- `make run` で実際に遊べる
- READMEにビルド方法、実行方法、遊び方が書かれている
- 実行ファイル `kumdor_01` がGit管理に入っていない
- LICENSEを追加している
- 可能ならプレイ例またはスクリーンショットをREADMEに載せる
