# クムドールの試練

Version: `v0.1.0`

C言語製のターミナルタイピングRPGです。

表示された課題をそのまま正確に入力して敵を攻撃します。10個のステージで敵を順番に倒せば完全勝利、途中でプレイヤーのHPが0になると敗北です。

ステージごとに練習内容が変わります。最初はホームポジションの `f` / `j` / スペースから始まり、ホーム段、上段、下段、数字、Shift記号へ段階的に進みます。
各ステージでは単キーだけでなく、左右交互、短い単語、スペース入りの課題も出題されます。

## 現在の内容

- C言語製のターミナルタイピングRPG
- プレイヤーと敵を `struct` で管理
- ターン制バトル
- プレイヤーHPと敵HP
- 複数の敵
- 10ステージ制
- ステージ別のタイピング練習内容
- 各ステージの複数課題パターン
- ステージ開始、戦闘開始、ステージ突破を分けたログ表示
- スペースを含む入力課題
- ステージ突破時の回復報酬
- ステージ突破時の経験値獲得
- レベルアップによる最大HP上昇
- 正解数、ミス数、命中率のスコア表示
- ステージ突破時の自動セーブ
- 起動時のセーブデータロード
- タイプミス時の敵の反撃
- 毒と暗闇の状態異常
- 大文字・小文字を区別したタイピング判定
- 英字、数字、記号を含む課題の出題

## 必要な環境

- Cコンパイラ
- `make`

macOSでは、Command Line Tools が入っていればビルドできます。

## ビルド

```sh
make
```

以下の実行ファイルが作成されます。

```text
kumdor_01
```

## 実行

```sh
make run
```

または、直接実行できます。

```sh
./kumdor_01
```

セーブデータがある場合は、起動時にロードするか確認されます。

```text
セーブデータが見つかりました。ロードしますか？ (y/n):
```

セーブデータは `kumdor_save.txt` に保存されます。このファイルはGit管理しません。

ゲーム中に中断したい場合は、入力欄でコマンドを入力できます。

| コマンド | 内容 |
| --- | --- |
| `:save` | 現在のステージの先頭から再開できる状態で保存 |
| `:quit` | 保存せずに終了 |
| `:savequit` | 保存して終了 |

中断セーブでは敵HPの途中状態は保存せず、現在ステージの先頭から再開します。

## ステージ構成

| ステージ | 練習内容 |
| --- | --- |
| 1 | ホームポジション: `f` / `j` / スペース |
| 2 | 左手ホーム段: `a s d f` |
| 3 | 右手ホーム段: `j k l ;` |
| 4 | 人差し指の上段: `r t y u` |
| 5 | 上段の広がり: `q w e i o p` |
| 6 | 中央から下段: `g h v b n m` |
| 7 | 下段と記号: `z x c , . / ?` |
| 8 | 総合練習: 大文字・小文字・記号 |
| 9 | 数字キー: `1` から `0` |
| 10 | 最終練習: Shift記号と混合課題 |

## バランス方針

序盤はミスしても立て直しやすく、後半は長い課題や記号入力に集中できるようにしています。敵の反撃はタイプミス時だけ発生し、ステージ突破やレベルアップでHPを回復できます。

## プレイ例

```text
=========================================
【第1/10ステージ】
練習内容: ホームポジション: f / j / スペース
指使い  : 左右の人差し指をfとjに置き、右親指でスペースを押す。
敵      : クムドールの影
=========================================

*************** BATTLE START ***************
クムドールの影が立ちはだかった！
********************************************

あなた Lv:1 EXP:0/10 HP: 10/10 | クムドールの影 HP: 3/3
敵の構え: [ f j ]
課題を入力してEnter（:save/:quit/:savequit）: f j
➔ 見事なタイピング！ 剣が炸裂した！ (敵の残りHP: 2)
```

## クリーンアップ

```sh
make clean
```

生成された実行ファイルや一時的なビルド成果物を削除します。

## ファイル

| ファイル | 内容 |
| --- | --- |
| `kumdor_01.c` | 互換用のエントリポイント |
| `include/game.h` | 共有する定数、構造体、公開関数の宣言 |
| `src/game.c` | ゲーム進行、戦闘、セーブ、スコア表示 |
| `src/stages.c` | ステージ、敵、出題課題の定義 |
| `Makefile` | ビルド、実行、クリーンアップ用 |
| `LICENSE` | ライセンス |
| `.gitignore` | Git管理しない生成物の指定 |

## 生成されるファイル

| ファイル | 内容 |
| --- | --- |
| `kumdor_01` | ビルドされた実行ファイル |
| `kumdor_save.txt` | セーブデータ |

## 次の改善候補

まずはターミナル版として、1本の小さなRPGに育てます。

- ステージごとの課題バランス調整
- セーブデータ形式の拡張
- 戦闘やセーブ処理のさらなるファイル分割

## ライセンス

MIT License

## GitHub公開の目安

課題入力、複数の敵、段階的なステージ制、ライセンス、プレイ例、バランス調整、コード分割は入りました。小さな公開ゲームとしてビルドして遊べる状態です。

最低限、公開前に確認すること:

- `make` で警告なしにビルドできる
- `make run` でゲームを開始できる
- 勝利と敗北の両方が自然に発生する
- READMEに遊び方とビルド方法が書かれている
- 実行ファイルなどの生成物がGit管理に入っていない
- ライセンスを追加している

現在は「ステージ1からボス撃破まで一通り遊べる」状態です。
